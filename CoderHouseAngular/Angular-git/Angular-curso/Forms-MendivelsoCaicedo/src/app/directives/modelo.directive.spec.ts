import { ModeloDirective } from './modelo.directive';

describe('ModeloDirective', () => {
  it('should create an instance', () => {
    const directive = new ModeloDirective();
    expect(directive).toBeTruthy();
  });
});
