import { MatriculasPipe } from './matriculas.pipe';

describe('MatriculasPipe', () => {
  it('create an instance', () => {
    const pipe = new MatriculasPipe();
    expect(pipe).toBeTruthy();
  });
});
