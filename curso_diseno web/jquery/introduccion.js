$(document).ready(function(){
    // $(selector).accion();
    // $ - Indicamoos que vamos a acceder a jquery.
    // selector-accedemos al elemento que queremos trabajar 
    // accion -lo que queremos hacer con el elemento seleccionado
    $('h1').hide();
});
//otra forma de hacerlo
$(function(){
    //codigo se ejecutara hasta que el documento haya cargado
});